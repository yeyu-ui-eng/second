{
  "name": "sales-performance-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Internal Sales Performance Tracking Platform for Fashion Company",
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd backend && npm run build",
    "start": "cd backend && node dist/app.js",
    "test": "cd backend && npm test",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:build": "docker-compose build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
